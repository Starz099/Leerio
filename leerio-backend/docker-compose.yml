services:
  app:
    build:
      context: .
      dockerfile: DockerFile
    container_name: leerio-backend
    restart: unless-stopped
    env_file:
      - .env
    # Ensure your .env sets PORT=3000 (or adjust both sides)
    ports:
      - "3000:3000"
